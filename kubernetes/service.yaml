apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe
  namespace: tic-tac-toe
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app: tic-tac-toe
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: tic-tac-toe